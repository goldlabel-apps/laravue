{
    "name": "vue-graphql",
    "version": "1.0.2",
    "scripts": {
        "start": "clear && cd vue-graphql && npm run dev",
        "test": "clear && cd vue-graphql && npm run test:unit",
        "deploy": "clear && firebase deploy"
    }
}
